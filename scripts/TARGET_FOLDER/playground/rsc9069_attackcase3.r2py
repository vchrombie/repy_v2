# clean up if the file exists.
if "testfile.txt" in listfiles():
    removefile("testfile.txt")

# create/open a file
myfile = openfile("testfile.txt",True)

# close file
myfile.close()

# use undo
myfile.undo() # should not raise any exceptions or errors