#writeat after file is closed

if "testfile.txt" in listfiles():
    removefile("testfile.txt")

# create a file
myfile = openfile("testfile.txt", True)
myfile.writeat("Hello, World", 0)
myfile.close()
myfile.writeat("Replace.", 0)
